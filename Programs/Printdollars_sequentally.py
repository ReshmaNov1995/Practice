dol='$'
n=5
lst=[]
for i in range(n):
    lst.append(dol)

print(lst)
print(''.join(lst)) # The join in Python takes all the elements of an iterable and joins them into a single string. Numeric are not allowed.